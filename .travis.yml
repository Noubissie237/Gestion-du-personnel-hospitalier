language: python

python:
  - "3.9"

install:
  - pip install --upgrade pip
  - pip install -r GestionPersonnel/requirements.txt

script:
  - python manage.py GestionPersonnel/test

notifications:
  email:
    recipients:
      - wilfried.noubissie@facsciences-uy1.cm
    on_success: always
    on_failure: always